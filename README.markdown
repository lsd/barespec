## Ruby 1.9 Bundler/Guard/RSpec skeleton ##

### This is particularly useful as an rspec sandbox ###

#### Installation & Usage ####
    git clone <repo>.git
    cd <repo>.git
    bundle install

In a separate terminal, run:
    guard
and hack away.

